# Tests run against a throwaway in-memory database, recreated for each test context.
spring.datasource.url=jdbc:h2:mem:quizquest-test;DB_CLOSE_DELAY=-1
spring.jpa.hibernate.ddl-auto=create-drop
spring.h2.console.enabled=false
spring.jackson.serialization.indent-output=false
